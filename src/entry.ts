
import { } from "@belongs/compiler/src/language/tlang"

const tlangEditor = document.getElementById("tlang-editor");
const mipsEditor = document.getElementById("mips-editor");
const btnCompile = document.getElementById("btn-compile");
const btnRun = document.getElementById("btn-run");
const clickEvtName = "click";

btnCompile.addEventListener(clickEvtName, ()=>{
    
});
